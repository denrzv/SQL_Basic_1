create table PERSONS (
                         name VARCHAR(100), surname VARCHAR(100), age INTEGER, phone_number INTEGER,
                          city_of_living VARCHAR(100), primary key (name, surname, age)
);